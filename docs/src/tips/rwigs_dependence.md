# RWIGS dependence

In spin-direction-constrained noncollinear calculations, a constraining field is applied to each atom.
The atomic region for this constraint is defined via the `RWIGS` parameter.
Here we show that the spin model parameters are fortunately not very sensitive to the choice of `RWIGS`.
