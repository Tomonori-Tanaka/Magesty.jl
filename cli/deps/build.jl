# Install the `magesty` command-line interface into ~/.julia/bin.
#
# Runs automatically on `import Pkg; Pkg.build("MagestyCLI")`, and can be
# invoked directly:
#
#     julia --project deps/build.jl
#
# `comonicon_install` is generated by the `Comonicon.@main` macro in
# `src/MagestyCLI.jl`. Add ~/.julia/bin to PATH to use the command.

using MagestyCLI

MagestyCLI.comonicon_install()
